{
  "name": "stock-app-oneclick",
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "build": "npm run build:web && npx prisma generate",
    "build:web": "cd web && npm install && npm run build",
    "start": "npx prisma migrate deploy || npx prisma db push; node src/index.js"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "prisma": "^5.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}